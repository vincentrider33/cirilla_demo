///
/// Value is "logo" || "full"
const String splashScreenType = 'logo';

///
/// String hex color. Default white
const String splashScreenColor = '#FFFFFF';

///
/// Type is "fill" || "fit-width" || "fit-height" || "scale-down" || "contain" || "none" || "cover"
const String splashScreenBoxFit = 'fit-width';
const double splashScreenLogoWidth = 200;
const double splashScreenLogoHeight = 200;
